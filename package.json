{
  "name": "@board-bound/workspace",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node index.js",
    "postinstall": "if [ ! -d \"server\" ]; then git clone git@github.com:board-bound/server.git; fi"
  },
  "devDependencies": {
    "chokidar": "^4.0.1",
    "inquirer": "^12.0.1",
    "picomatch": "^4.0.2"
  }
}
